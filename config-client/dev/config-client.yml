spring:
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/IvanCherepica/ms-config-properties
          search-paths:
            - "/{application}/{profile}"